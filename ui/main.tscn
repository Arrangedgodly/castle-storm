; The game's front door — the boot shell (post-acceptance fix): boots
; the REAL GameHost against user://saves and routes title/continue/
; new-run into the Spread. What Play/F5 runs, exactly what the exported
; builds run, and what `make run-game` runs.
[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://ui/main.gd" id="1_main"]

[node name="Main" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
script = ExtResource("1_main")

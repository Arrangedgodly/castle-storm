; The chronicle screen (T-UI-08): past runs as spread history, composed
; by the Spread as paper over the table while open (never modal chrome).
; Headless-testable; opened from the run header's chronicle chip
; (`make run-game`, the ledger chip on the letterhead strip).
[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://ui/screens/chronicle/chronicle_screen.gd" id="1_chronicle"]

[node name="ChronicleScreen" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
script = ExtResource("1_chronicle")

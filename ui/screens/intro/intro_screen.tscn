; The leader intro / restart reveal (T-UI-05): the identity deal + the
; one-gesture unfold open, composed by the Spread as paper over the table
; while open (never modal chrome). Headless-testable; mounted by the
; spread at first boot, from the assault's finished("win") seam, and off
; run_lost/run_aborted (see spread_screen.gd).
[gd_scene load_steps=2 format=3]

[ext_resource type="Script" path="res://ui/screens/intro/intro_screen.gd" id="1_intro"]

[node name="IntroScreen" type="Control"]
layout_mode = 3
anchors_preset = 15
anchor_right = 1.0
anchor_bottom = 1.0
grow_horizontal = 2
grow_vertical = 2
script = ExtResource("1_intro")
